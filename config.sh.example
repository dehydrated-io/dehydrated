# default values are commented

#CA="https://acme-v01.api.letsencrypt.org"
#WELLKNOWN=".acme-challenges"
#KEYSIZE=4096
#BASEDIR=./
#OPENSSL_CNF=.... # system default (see openssl version -d)
#ROOTCERT="lets-encrypt-x1-cross-signed.pem"

# program called before responding to the challenge, or after failed challenge
# or after creating certs to upload certs 
# arguments: path/to/token action  path/to/token token
# [path/to/fullchain] [path/to/privkey] [path/to/cert]
# action can be upload_challenge, clean_challenge, upload_certs
# token; can be used to e.g. upload the challenge if this script doesn't run
# on the webserver
#HOOK=

# try to renew certs that are within RENEW_DAYS days of their expiration date
#RENEW_DAYS="14"

# create new private key for each csr (yes|no)
#PRIVATE_KEY_RENEW=no

